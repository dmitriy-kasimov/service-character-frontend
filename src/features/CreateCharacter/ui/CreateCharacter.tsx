import { FC } from 'react';
import { Button, Text } from '@tr271v0r/ui-tool-kit';

export const CreateCharacter: FC = () => {
    return (
        <Button>
            <Text>Create</Text>
        </Button>
    );
};
